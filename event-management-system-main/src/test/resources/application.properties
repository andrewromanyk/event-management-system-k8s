building.service.url=http://localhost:8080

jwt.secret=Gw9OmMQ3mAiEV1OFeFiJWxnIGpIKsAMh
secret.name=name-main
secret.pass=pass-main
activemq.custom.enable = false

# Disable Spring Cloud Config in tests
spring.cloud.config.enabled=false
spring.config.import=optional:

# Disable ActiveMQ/JMS
spring.jms.enabled=false
spring.activemq.broker-url=vm://localhost?broker.persistent=false

# Use H2 for tests
spring.datasource.url=jdbc:h2:mem:testdb
spring.jpa.hibernate.ddl-auto=create-drop

# Disable gRPC if you have it
grpc.client.building-service.address=static://localhost:9090
grpc.client.building-service.negotiationType=PLAINTEXT

# Disable actuator health checks that might fail
management.health.jms.enabled=false
management.health.mail.enabled=false